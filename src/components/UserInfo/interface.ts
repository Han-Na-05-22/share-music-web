export interface UserInfoProps {
  className?: string;
}

export interface UserFormProps {
  photoURL: string;
  name: string;
  pwd: string;
  rePwd: string;
  phoneNumber: string;
  displayName: string;
}
